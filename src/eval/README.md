This folder contains code that is submitted as course assignment.

These code below are directly copied from TA code repo and the only modification are `# nofqa` etc. to prevent ruff auto formatting and linting

```python
# ruff: noqa
# fmt: off
```

```bash
source ./.venv/bin/activate
cd ./src/eval
python3 evaluate.py
```
